# ZELLIJ_EXEC can be set locally - it can either be empty, or 'exec' (the default)
# when empty, exiting zellij will drop to the default shell
# when 'exec', zellij binary will replace the login shell, meaning that whe exiting
# zellij, terminal connection will close.

# exec shoud only be set as a default, otherwise it would break any following zellij invocation
ZELLIJ_EXEC=${ZELLIJ_EXEC:-exec}
#SZ_NOMUX=1
